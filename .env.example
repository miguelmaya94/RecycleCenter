# Example environment variables for CycleiQ

# MongoDB Atlas connection string (include a DB name at the end)
# Format: mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority&appName=<app>
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER_HOST/cycleiq?retryWrites=true&w=majority&appName=Cluster0

# Long random string for session signing (DO NOT reuse your DB password)
SESSION_SECRET=change-me-to-a-long-random-string
